/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            protocol: 'https',
            hostname: 'fuqsybc0zaryo1cl.public.blob.vercel-storage.com',
            port: '',
        }, ],
    },
};

export default nextConfig;